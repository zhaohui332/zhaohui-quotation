# 极空间 Docker Compose 部署

## 方式一：极空间 Docker 界面

1. 在极空间里新建一个文件夹，例如 `zhaohui-quotation`。
2. 把本仓库 `Dockerfile`、`compose.yml`、`pdf_export.py`、`server.js`、`package.json`、`requirements.txt`、`index.html`、`assets/` 一起放到这个文件夹。
3. 极空间 Docker 中选择“项目 / Compose”，粘贴或选择上面的 `compose.yml` 后启动。
4. 启动后访问：`http://极空间IP:4731`

数据会保存到项目文件夹下的 `zhaohui-data/data` 和 `zhaohui-data/assets`，升级或重启不会丢失。

## 方式二：命令行

```bash
docker compose up -d --build
```

停止：

```bash
docker compose down
```

## 说明

- 服务监听 `4731` 端口，可在 `compose.yml` 中改成你 NAS 上的空闲端口。
- 已内置中文字体和 Word/PDF 导出能力。
- 建议只在可信局域网或配合极空间反向代理使用；公网直连需要自行加访问密码或防火墙。
